we need
ask how many student
leave space for every student
save them seperate

names should be connected with grades

struct student { 
char name[50];
char grade[2]; 
    }; 




#include <stdio.h>
#include <string.h>
struct employee {                                       emp2 
char name[50];
char TCKN[11]; 
float salary;
}; 
int main() { 
struct employee emp1={","10165789200",10000.0}, emp2={"Tuba Aslan Albayrak","52367890104",7500.0}; 
printf("%s\n",strcpy(emp1.name,"Murat Kurdoglu"));
printf("Name change to %s\n",strcpy(emp2.name,"Mehmet Cenkoglu"));
emp1.salary = 10000.0;
puts(emp1.name);
Puts(emp2.name);
    return 0; 


int main()
{
   struct Point arr[10];
   arr[0].x = 2;	arr[0].y = -3;
   printf(‘‘x-coord = %d, y-coord =%d’’, arr[0].x , arr[0].x );
   return 0;
}


